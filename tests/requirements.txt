# SPDX-FileCopyrightText: 2026 Alexander Merose <al@merose.com> & ddx Authors
#
# SPDX-License-Identifier: Apache-2.0

# What the cross-engine oracle suite needs, on top of a built `ddxdb` wheel
# (see this directory's README for the two commands).
#
# Not a package: these tests are not distributed, they exercise ddx against
# other people's engines. A requirements file says that plainly, where a
# pyproject.toml would imply something installable.

jax>=0.4          # the oracle, and the jaxpr IR the harness interprets
pytest>=7
duckdb>=1.0       # the two engines the rewrite is checked on
datafusion>=45
pyarrow>=14       # how row fixtures reach both engines with identical dtypes
numpy>=1.24
